import 'package:flutter/material.dart';

class ProfileDeclScreen extends StatelessWidget {
  const ProfileDeclScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ProfileScreen')),
    );
  }
}